const primaryModel = "gpt-4o";
const fallbackModel = "gpt-3.5-turbo";

const primaryAgent = { model: primaryModel, name: "Primary" };
const fallbackAgent = { model: fallbackModel, name: "Fallback" };
